#include <bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while (t--) {
        int n, p;
        cin >> n >> p;
        vector<int> a1(n), b1(n);
        for (int i = 0; i < n; i++) {
            cin >> a1[i];
        }
        for (int i = 0; i < n; i++) {
            cin >> b1[i];
        }
        unordered_map<int, int> mp;
    }
}