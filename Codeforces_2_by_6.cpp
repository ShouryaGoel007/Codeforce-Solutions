# include  <bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--) {
        int n;
        cin>>n;
        // use a while loop with n*2%6 should be approaching towards 0 only then we can get may be some logic like that/
    }
}    